# Реліз 2.1.196 — 2026-07-07

## Нові можливості

- Organization default models — адміни встановлюють модель за замовчуванням в org console
- Зрозумілі імена сесій при старті
- Клікабельні вкладення файлів у чаті (Cmd/Ctrl-click відкриває в Finder/Explorer)
- Streaming idle watchdog увімкнений за замовчуванням для всіх провайдерів
- Remote Control автоматично вимикається для non-Anthropic `ANTHROPIC_BASE_URL`
- Відкриття agents view з foreground сесії тепер вимагає одного `←`

## Виправлення

- `claude mcp list`/`get` більше не запускають недовірені `.mcp.json` сервери
- Фіксація permanent deletion транскрипту при wake background job
- Rate-limit warning миготіння та перерахунок телеметрії
- Дублікати recap-рядки після background session turn
- PowerShell `git diff`/`git grep`/`egrep`/`fgrep` exit 1 на Windows
- Безліч焦点 keyboard focus в agents view, втрата subagent types
- `--dangerously-skip-permissions` мовчазки відступав до auto mode
- Crash recovery Remote sessions при рестарті сервера
- `/cd` сесії з special characters в resume list
- `claude plugin validate` пропускав локальні плагіни
- Esc Esc rewind menu regression
- MCP OAuth `invalid_scope` на enterprise IdPs
- `/context` показував 0 токенів на Bedrock
- `/deep-research` хибне звітування verifier failures
- Plugin dependency version pins
- Voice dictation ковтання пробілів
- Background sessions виживають stop/restart/update
- Agents auto-resume після daemon restart
- `/code-review` workflow: об'єднано cleanup finders (−25% токенів)
- Reduced per-frame rendering work
- `claude agents` session status: завершені без мерцтання, зупинені з "Needs attention"

## Важливі зміни

- `CLAUDE_ENABLE_STREAM_WATCHDOG=0` тепер вимикає watchdog (раніше opt-in)
