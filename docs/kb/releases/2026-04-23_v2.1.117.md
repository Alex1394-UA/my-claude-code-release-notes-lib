# Реліз 2.1.117 — 2026-04-23

## Нові можливості
- `CLAUDE_CODE_FORK_SUBAGENT=1` для external builds (категорія: env-vars, core)
- Agent frontmatter `mcpServers` для `--agent` (категорія: skills-agents)
- `/model` persist across restarts + startup header source (категорія: core)
- `/resume` summarize stale sessions (категорія: cli)
- Concurrent MCP connect (local + claude.ai) (категорія: mcp)
- Plugin install missing deps re-resolve (категорія: plugins)
- Plugin marketplace auto-resolve dependencies (категорія: plugins)
- `blockedMarketplaces`/`strictKnownMarketplaces` enforcement (категорія: plugins)
- Advisor Tool experimental з learn-more (категорія: core)
- `cleanupPeriodDays` розширено на tasks/shell-snapshots/backups (категорія: core)
- Native builds `bfs`/`ugrep` для Glob/Grep [macOS/Linux] (категорія: core)
- Default effort = high для Pro/Max на Opus 4.6/Sonnet 4.6 (категорія: core)
- OTEL enhanced attributes (effort, command_name) (категорія: env-vars)
- Cached `where.exe` lookups [Win] (категорія: windows)

## Виправлення
- Login token reactive refresh на 401 (sdk)
- `WebFetch` hang на великих HTML сторінках (core)
- HTTP 204 No Content crash (core)
- `/login` no effect з `CLAUDE_CODE_OAUTH_TOKEN` (sdk)
- Prompt undo `Ctrl+_` (core)
- `NO_PROXY` не поважався під Bun (env-vars)
- Rare key coalesced triggers (core)
- SDK `reload_plugins` serial reconnect (sdk)
- Bedrock thinking disabled 400 (sdk)
- MCP elicitation auto-cancel print/SDK (mcp)
- Subagent model mismatch malware warning (core)
- Idle render loop memory (core)
- VS Code "Manage Plugins" panel (vscode-ide)
- Opus 4.7 context window 200K→1M fix (core)
