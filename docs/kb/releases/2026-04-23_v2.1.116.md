# Реліз 2.1.116 — 2026-04-23

## Нові можливості
- `/resume` perf на великих сесіях (до 67% на 40MB+) (категорія: cli)
- MCP startup прискорення — `resources/templates/list` deferred (категорія: mcp)
- Fullscreen scroll в VS Code/Cursor/Windsurf (категорія: vscode-ide)
- Thinking spinner з inline прогресом (категорія: core)
- `/config` search match option values (категорія: core)
- `/doctor` під час відповіді (категорія: core)
- `/reload-plugins` auto-install missing dependencies (категорія: plugins)
- Bash `gh` rate limit hint (категорія: core)
- Usage tab immediate data display (категорія: core)
- Agent frontmatter `hooks:` fire для `--agent` (категорія: skills-agents)
- No-commands-match message в slash menu (категорія: core)
- Claude Code download URL `downloads.claude.ai` (категорія: cli)

## Виправлення
- Sandbox auto-allow dangerous-path bypass (settings)
- Devanagari/Indic scripts rendering (core)
- `Ctrl+-` undo Kitty keyboard protocol (keybindings)
- `Cmd+Left/Right` Kitty keyboard protocol (keybindings)
- `Ctrl+Z` hang через wrapper process (core)
- Inline scrollback duplication (core)
- Modal search overflow на коротких терміналах (core)
- VS Code scroll cells/blanks (vscode-ide)
- API 400 cache TTL ordering (core)
- `/branch` >50MB transcript rejection (cli)
- `/resume` empty session on large files (cli)
- `/plugin` duplicate Installed tab (plugins)
- `/update`/`/tui` worktree fix (cli)
