# Реліз 2.1.139 — 2026-05-12

## Нові можливості
- Agent view (Research Preview) — єдиний список всіх сесій через `claude agents` (категорія: core, cli, skills-agents)
- `/goal` command — completion condition, Claude працює across turns (категорія: core, skills-agents)
- `/scroll-speed` command — налаштування mouse wheel scroll speed з live preview (категорія: core, keybindings)
- `claude plugin details <name>` — component inventory та token cost (категорія: cli, plugins)
- Transcript view navigation: `?`, `{`/`}`, `v` (категорія: keybindings)
- Hook `args: string[]` (exec form) — spawn без shell (категорія: hooks)
- Hook `continueOnBlock` для PostToolUse (категорія: hooks)
- MCP stdio `CLAUDE_PROJECT_DIR` environment variable (категорія: mcp, env-vars)
- Compaction prompt preserves sensitive instructions (категорія: core)
- `/mcp` Reconnect picks up `.mcp.json` edits (категорія: mcp)
- `/context all` per-skill token estimates з tokenizer (категорія: core)
- `claude plugin install <name>@<marketplace>` auto-refresh (категорія: cli, plugins)
- `/plugin` installed details show hooks/MCP (категорія: plugins)
- `/context` shows plugin name for plugin-sourced skills (категорія: core)
- Remote MCP reconnect retry for all users (категорія: mcp)
- Subagent `x-claude-code-agent-id` headers + OTEL attributes (категорія: sdk)
- [VSCode] Cmd/Ctrl+Shift+T reopen closed session tab (категорія: vscode-ide)

## Виправлення
- Deadlock `forceRemoteSettingsRefresh` + expired credentials (категорія: settings, core)
- `autoAllowBashIfSandboxed` shell expansions (категорія: settings)
- Hook terminal corruption (категорія: hooks)
- HTTP/SSE MCP non-protocol data unbounded memory (категорія: mcp)
- `Skill(name *)` wildcard permission rules (категорія: settings, skills-agents)
- Settings symlink hot-reload (категорія: settings)
- Plugin details marketplace key mismatch (категорія: plugins)
- `/model` picker Default row model overrides (категорія: core)
- Stream idle timeout spurious 5min (категорія: core)
- Silent exit 1 unwritable MCP cache (категорія: mcp)
- Typing cursor blinking dialogs (категорія: core)
- Transcript view shortcuts after mouse click (категорія: keybindings)
- Bash-mode up-arrow history (категорія: core)
- Multiple images paste/drop (категорія: core)
- Hyperlinks dark themes readability (категорія: core)
- Model picker redundant Current model 3P (категорія: core)
- Legacy Opus PAYG 3P same model (категорія: core)
- Mouse wheel scrolling Cursor/VS Code (категорія: core, vscode-ide)
- Scroll Win Terminal + VS Code background (категорія: windows, vscode-ide)
- MCP resources disconnected servers lingering (категорія: mcp)
- Two-file diff truncation count (категорія: core)
- Grep Windows drive-letter paths (категорія: windows)
- Border text CJK/emoji overflow (категорія: core)
- Fuzzy-match emoji/astral splitting (категорія: core)
- Skill argument regex metacharacters (категорія: skills-agents)
- ProgressBar fractional cell (категорія: core)
- Task polling/fs.watch resurrected (категорія: core)
- Plugin dependency resolution stale count (категорія: plugins)
- Insights Time-of-Day chart skewing (категорія: core)
- Keybindings cmd/super/win unparseable (категорія: keybindings)
- `claude_code.active_time.total` OTEL `--print` (категорія: env-vars)
- `claude plugin update` cross-plugin symlinks (категорія: plugins)

## Важливі зміни
- RC/schedule/connectors/disabled коли API key встановлено, навіть при наявності Claude.ai login
