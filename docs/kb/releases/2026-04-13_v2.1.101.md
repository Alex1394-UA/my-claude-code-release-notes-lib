# Реліз 2.1.101 — 2026-04-13

## Нові можливості
- `/team-onboarding` — генерація teammate ramp-up guide (категорія: skills-agents)
- OS CA certificate store trust за замовч. (enterprise TLS proxies) (категорія: env-vars)
- `/ultraplan` auto-create default cloud environment (категорія: core)

## Виправлення
- `claude -p --resume <name>` приймає session titles (категорія: cli)
- `--resume`/`--continue` втрата контексту на великих сесіях (категорія: cli)
- `--resume` chain recovery в несуміжну subagent розмову (категорія: cli)
- `--resume` crash при відсутньому file_path (категорія: cli)
- Hardcoded 5-хв request timeout ігнорував `API_TIMEOUT_MS` (категорія: env-vars)
- `permissions.deny` не перезаписував hook `ask` (категорія: hooks)
- `--setting-sources` без `user` видаляв історію >30 днів (категорія: settings)
- Bedrock SigV4 403 з AUTH_TOKEN/apiKeyHelper/CUSTOM_HEADERS (категорія: core)
- `claude -w` stale worktree (категорія: core)
- Subagents не наслідували dynamic MCP tools (категорія: core)
- Sub-agents в worktree denied Read/Edit (категорія: core)
- `claude mcp serve` outputSchema validation (категорія: mcp)
- RemoteTrigger `run` empty body (категорія: sdk)
- Grep ENOENT stale ripgrep binary (категорія: core)
- `/btw` писав повну розмову на диск (категорія: core)
- `/context` Free space/messages disagreement (категорія: core)
- Plugin issues: duplicate name, ENAMETOOLONG, Discover, cache, skills (категорія: plugins)
- `/mcp` OAuth для `headersHelper` → Reconnect (категорія: mcp)
- `ctrl+]`/`ctrl+\`/`ctrl+^` raw C0 terminals (категорія: core)
- `/login` OAuth URL padding (категорія: cli)
- Rendering: flicker, scrollback wipe, mouse-scroll leak (категорія: core)
- settings.json env numbers crash (категорія: settings)
- In-app settings writes in-memory refresh (категорія: settings)
- Custom keybindings не завантажувались на Bedrock/Vertex/3p (категорія: settings)
- `claude --continue -p` не продовжував -p/SDK сесії (категорія: cli)
- Remote Control: worktrees, transcript, brief mode, SSH (категорія: sdk)
- `/insights` missing report link (категорія: core)
- [VSCode] File attachment not clearing (категорія: vscode-ide)
- Command injection в POSIX `which` fallback (категорія: core)
- Memory leak message list на довгих сесіях (категорія: core)

## Важливі зміни
- Brief mode retry на plain text (категорія: core)
- Focus mode self-contained summaries (категорія: core)
- Rate-limit retry показує який ліміт та коли скидається (категорія: core)
- Refusal error messages включають API explanation (категорія: core)
- Tool-not-available error пояснення (категорія: core)
- Unrecognized hook event name не ігнорує settings.json (категорія: hooks)
- Plugin hooks managed settings `allowManagedHooksOnly` (категорія: plugins)
- `/plugin update` stale marketplace warning (категорія: plugins)
- SDK `query()` cleanup subprocess/temp files (категорія: sdk)
- Beta tracing honors `OTEL_LOG_USER_PROMPTS`/`OTEL_LOG_TOOL_CONTENT` (категорія: env-vars)
