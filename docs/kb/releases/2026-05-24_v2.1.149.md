# Реліз 2.1.149 — 2026-05-24

## Нові можливості
- `/usage` per-category breakdown — розбивка по skills, subagents, plugins, per-MCP-server cost (категорія: core)
- `/diff` keyboard scrollable detail view — arrows, j/k, PgUp/PgDn, Space, Home/End (категорія: cli)
- GFM task list checkboxes rendering — `- [ ]` / `- [x]` в markdown (категорія: core)
- `allowAllClaudeAiMcps` managed setting — завантаження claude.ai MCP connectors з managed-mcp.json (категорія: settings)

## Виправлення
- PowerShell `cd` functions permission bypass (`cd..`, `cd\`, `cd~`, `X:`) (категорія: settings, windows)
- PowerShell prefix/wildcard allow rules не pre-approving native executables (категорія: settings, windows)
- Permission-analysis stale PWD/OLDPWD/DIRSTACK values (категорія: settings)
- Sandbox write allowlist git worktree covering entire repo root (категорія: settings)
- `find` вичерпання macOS vnode table на великих directory trees (категорія: cli)
- Managed-settings approval dialog freeze при startup (категорія: settings)
- `/ultraplan` "Could not capture uncommitted changes" false positive (категорія: cli)
- `otelHeadersHelper` silent failure при пробілах в script path (категорія: cli)
- Thinking spinner amber across tool calls (категорія: core)
- Status bar baseline effort замість skill/agent effort (категорія: core)
- Ctrl+O transcript view freeze при відкритті (категорія: core)
- Prompt-history edit losing при навігації arrow keys (категорія: core)
- `/config` phantom changes для auto-compact та theme (категорія: cli)
- `/insights` crash при missing optional fields (категорія: cli)
- Malformed PowerShell/History misclassified як reads (категорія: cli)
- Remote Control session rename не оновлював local session name (категорія: sdk)
- Up-arrow history race — дублювання щойно відправленого промпту (категорія: core)
- "Jump to bottom" pill не dismiss-ався в fullscreen (категорія: core)
- Collapsed Bash output wrong hidden-line count (категорія: core)
- Argument-hint clipping trailing typed characters (категорія: core)
- Argument-hint не з'являвся після Tab-completing skill з іншим `name:` (категорія: skills-agents)
- `/feedback` reports тепер включають pre-compaction conversation (категорія: cli)

## Важливі зміни
- Немає
