# Реліз 2.1.89 — 2026-04-01

## Нові можливості
- `"defer"` permission decision для PreToolUse хуків — головні сесії можуть поставити на паузу і відновити з `-p --resume` (hooks)
- `PermissionDenied` hook event — спрацьовує при відмові auto mode; `retry: true` для повтору (hooks)
- `CLAUDE_CODE_NO_FLICKER=1` — flicker-free alt-screen rendering з virtualized scrollback (env-vars)
- `MCP_CONNECTION_NONBLOCKING=true` — пропустити очікування MCP з'єднання в `-p` mode (env-vars, mcp)
- `showThinkingSummaries` setting — thinking summaries вимкнено за замовчуванням (settings, core)
- Автокомпакт thrash detection — зупинка після 3 послідовних невдач (core)
- Edit tool для файлів з Bash `sed`/`cat` — без окремого Read (core)
- Named subagents в `@`-mention typeahead (skills-agents, core)
- Bash formatter warning — попередження при зміні раніше прочитаних файлів (core)
- Auto mode denied commands notification + `/permissions` Recent tab (core)
- `!command` paste entry в bash mode (core)
- Image paste без trailing space (core)
- Collapsed tool summary "Listed N directories" для ls/tree/du (core)
- Task notifications зберігаються при Ctrl+B backgrounding (core)
- Hook output >50K зберігається на диск з прев'ю (hooks)
- `file_path` як абсолютний шлях в PreToolUse/PostToolUse (hooks)
- Hooks `if` тепер match compound commands та env-var prefixes (hooks)
- `-p --resume` deferred tools re-evaluation + `--mcp-config` bounded at 5s (cli)

## Виправлення
- Edit/Write CRLF doubling на Windows (windows)
- Shift+Enter на Windows Terminal Preview 1.25 (windows)
- PowerShell tool PS 5.1 false failure reporting (windows)
- MCP tool errors truncation (mcp)
- `claude-cli://` deep links на macOS (cli)
- Prompt cache misses в long sessions (core)
- Nested CLAUDE.md re-injection dozens of times (core)
- `--resume` crash для older CLI version transcripts (cli)
- Voice push-to-talk modifier-combo bindings (core)
- Voice mic permission на macOS Apple Silicon (core)

## Важливі зміни
- Thinking summaries **вимкнено за замовчуванням** — `showThinkingSummaries: true` для відновлення
- `cleanupPeriodDays: 0` тепер відхиляється з помилкою валідації
- `Edit(//path/**)` / `Read(//path/**)` allow rules перевіряють resolved symlink target
