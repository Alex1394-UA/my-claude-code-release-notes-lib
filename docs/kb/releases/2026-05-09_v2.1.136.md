# Реліз 2.1.136 — 2026-05-09

## Нові можливості
- `CLAUDE_CODE_ENABLE_FEEDBACK_SURVEY_FOR_OTEL` — повторне ввімкнення session quality survey для OTEL (категорія: env-vars)
- `settings.autoMode.hard_deny` — безумовні auto mode deny правила (категорія: settings)

## Виправлення
- MCP servers disappearing після `/clear` в VS Code, JetBrains, SDK (категорія: mcp, vscode-ide, sdk)
- Login loop з concurrent credential write (категорія: core)
- MCP OAuth refresh tokens lost при concurrent refresh (категорія: mcp)
- API 400 з redacted thinking block після tool call (категорія: core)
- `--resume`/`--continue` з underscores в project path (категорія: cli)
- Plan mode не blocking file writes з Edit allow rule (категорія: core, settings)
- WSL2 image paste PowerShell fallback (категорія: windows)
- Plugin hooks failing при cache cleanup (категорія: plugins, hooks)
- Colors в bash output та markdown code blocks (категорія: core)
- ReasonML diffs "undefined" artifacts (категорія: core)
- Worktree exit dialog wrong directory (категорія: cli)
- `@` file picker mid-session та >100 entries (категорія: core)
- Failed tool calls не expandable в fullscreen (категорія: core)
- Backspace/Ctrl+Backspace swapped після Ctrl+G (категорія: keybindings)
- `/usage` weekly reset time of day (категорія: cli)
- Welcome banner ellipsis CJK overflow (категорія: core)
- `/insights` crash з malformed input (категорія: cli)
- Renderer crash collapsibility change (категорія: core)
- Plugin.json `skills` hiding default `skills/` (категорія: plugins, skills-agents)
- IDE shell-integration lock files `CLAUDE_CONFIG_DIR` (категорія: vscode-ide, env-vars)
- Trailing whitespace в copied output (категорія: core)
- Plugin uninstall case-insensitive matching (категорія: plugins)
- Tool error truncation negative count (категорія: core)
- `CLAUDE_ENV_FILE` SessionStart hooks stale (категорія: hooks, env-vars)
- `/branch` multi-line title (категорія: cli)
- Stray leading space wrapped text (категорія: core)
- Esc не dismissing dialogs (категорія: keybindings)
- `/doctor` MCP schema errors (категорія: cli)
- Bash permission prompts parser diagnostic (категорія: settings)
- Plugin slash commands з spaces (категорія: plugins)
- `AskUserQuestion` discarding multi-select (категорія: skills-agents)
- `/clear <name>` не labeling session (категорія: cli)
- `CronList` output missing qualifiers (категорія: skills-agents)
- "Jump to bottom" CJK artifacts (категорія: core)
- Wide markdown tables stale render (категорія: core)
- Pasted text dropped на auto-truncation (категорія: core)
- `/release-notes` stuck на old version (категорія: cli)
- `/mcp` server list не scrolling (категорія: mcp)
- Mid-input slash autocomplete (категорія: cli)
- Scrolling re-engaging auto-follow (категорія: core)
- Prompt suggestions auto-submitted (категорія: core)
- Keyboard shortcut hints не reflecting keybindings.json (категорія: keybindings)
- `/settings` language change reverted на Escape (категорія: cli)
- `/terminal-setup` autocomplete exact match (категорія: cli)
- "Chat about this" на AskUserQuestion erasing question (категорія: skills-agents)
- MCP tool results invisible з content blocks (категорія: mcp)

## Важливі зміни
- Plugin marketplace removal key змінено з `r` на `d` (matching delete pattern)
- Покращено visual consistency slash command dialogs
- Покращено worktree collision error message
