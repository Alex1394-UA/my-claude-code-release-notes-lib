# Реліз 2.1.122 — 2026-05-04

## Нові можливості
- `ANTHROPIC_BEDROCK_SERVICE_TIER` змінна для вибору Bedrock service tier (категорія: env-vars)
- `/resume` тепер знаходить сесію по PR URL (GitHub, GitHub Enterprise, GitLab, Bitbucket) (категорія: cli)
- `/mcp` показує приховані claude.ai connectors з підказкою видалити дублікат (категорія: mcp)
- OTEL: числові атрибути як числа (категорія: env-vars)
- OTEL: новий `claude_code.at_mention` log event (категорія: env-vars)

## Виправлення
- `/branch` форки з rewound timelines (категорія: core)
- `/model` Effort опція для Bedrock ARNs (категорія: core)
- Vertex AI / Bedrock `output_config: Extra inputs are not permitted` (категорія: core, hooks)
- Vertex AI `count_tokens` 400 за proxy gateways (категорія: core)
- `spinnerTipsOverride.excludeDefault` time-based tips (категорія: settings)
- ToolSearch пропускав MCP tools підключені після старту (категорія: mcp)
- `!exit` / `!quit` завершував CLI замість shell command (категорія: core)
- Зображення resized до 2576px замість 2000px (категорія: core)
- Remote control idle status подвійна перемальовка (категорія: sdk)
- Порожні повідомлення через stale view preference (категорія: core)
- Malformed hooks entry в settings.json (категорія: settings)
- Caps Lock keybinding в voice mode (категорія: keybindings)

## Важливі зміни
- Vertex AI/Bedrock hooks `output_config` помилку виправлено (відомий issue в hooks.md)
