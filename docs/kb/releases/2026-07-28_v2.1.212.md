# Реліз 2.1.212 — 2026-07-28

## Нові можливості

- `claude auto-mode reset` — нова підкоманда для відновлення типової конфігурації auto-mode з промптом підтвердження (категорія: cli)
- `CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` — нова змінна: сесійний ліміт викликів WebSearch (типово 200) (категорія: env-vars)
- `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` — нова змінна: сесійний ліміт породжень субагентів (типово 200), скидається через `/clear` (категорія: env-vars)
- `CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` — нова змінна: поріг автопереміщення MCP tool call у фон (категорія: env-vars)
- MCP auto-background для довгих викликів — виклики понад 2 хв переходять у фон автоматично (категорія: mcp)
- `/fork` → background copy + `/subtask` — `/fork` копіює розмову у нову фонову сесію, а внутрішньосесійний субагент став окремою `/subtask` (категорія: cli)
- `/resume` in agent view — відкриває picker минулих сесій (включно з видаленими) і відновлює вибір як фонову сесію (категорія: cli)

## Виправлення

- Plan mode file-modifying Bash без промпту дозволу або `canUseTool` (core)
- Створення worktree після symlink `.claude/worktrees` (settings)
- `continue:false` hook halt втрачався при завершенні інструменту посеред стрімінгу (hooks)
- SIGTERM під час Bash orphaning дерева процесів у print/SDK-режимі (cli)
- `/background` + `claude --bg` EUNKNOWN на Windows при заблокованому PowerShell 5.1 — daemon віддає перевагу PowerShell 7 (windows)
- Shell-режим `!` не виконував команд зі шляхами при відкритому автодоповненні (cli)
- Розбиті символи у сповіщеннях про відмову auto-mode при обрізанні посеред емодзі (core)
- Ctrl+J не вставляв новий рядок у agent view dispatch-input (keybindings)
- `/ultrareview` відхилення PR-посилань, неотримання гілки з origin, пропуск білінгу після `/clear`, підказка «not a git repository» на Claude Desktop (cli)
- Збій hosted-сесій при mTLS/CA/OAuth repository settings (settings)
- «File has not been read yet» при редагуванні файлу, прочитаного з offset/limit (core)
- `ExitWorktree` після поновлення сесії в print/SDK-режимі (sdk)
- Порожня сітка workflow-агентів для Remote Control клієнтів посеред виконання (sdk)
- Control-запити streaming-режиму позначались завершеними зарано (sdk)
- `/fork` втрата live-parent захисту після помилки запису стану (skills-agents)
- Мовчазний збій повторного відкриття зупиненої фонової сесії з agent view (skills-agents)
- Дублювання idle-сповіщень лідеру від зупиняємого тімейта (skills-agents)
- Розщеплення «ctrl+g to edit in editor» у футері діалогу схвалення плану (core)
- Welcome-банер зберігав старі ширини панелей після resize (core)
- Втрата номерів рядків та маркерів +/- у diff-прев'ю у вузьких макетах (core)
- @-mentions нічого не додавали після часткового читання файлу; plugin uninstall націлювався на хибний marketplace; хибний «Command timed out» на exit 143 (core, plugins)
- OpenTelemetry HTTP-експорти відхилялись 411/400 Azure Monitor (env-vars)
- OTLP event log без `trace_id`/`span_id` при `TRACEPARENT` у SDK/headless (env-vars)
- Хибна «Request too large» у розмовах з багатьма зображеннями (core)
- «API Error» як результати web search/fetch при перевантаженні API (core)
- Виправлено ранішу ноту (2.1.200) щодо синхронізованого виводу tmux (core)

## Важливі зміни

- Покращено надійність web search/fetch — повтор 529-помилок з backoff (core)
- Кешування промптів працює за LLM-шлюзами та кастомними base URL (core)
- Покращено attach фонового агента — cold-attach миттєво показує транскрипт (skills-agents)
- Зменшено витрату токенів у міжагентних повідомленнях `SendMessage` (skills-agents)
- `/fork` називає копію за промптом при сесії без заголовка (cli)
- bare `/btw` повторно відкриває панель side-question (cli)
- `←` футер пульсує «N done» при завершенні фонового агента (skills-agents)
- Enterprise `forceLoginMethod` застосовується для VS Code/SDK/setup-token/install-github-app (settings)
- Транскрипти записують reasoning effort для кожного повідомлення (core)
- Headless/SDK застосовують `set_model` посеред ходу (sdk)
- `claude agents --json` показує «Needs input» замість «Working» (cli)
- Заголовок панелі автентифікації → «Authentication» (cli)

## Застарівання

- Параметр `mode` інструменту Task — тепер ігнорується; субагенти успадковують permission mode батьківської сесії (skills-agents)
