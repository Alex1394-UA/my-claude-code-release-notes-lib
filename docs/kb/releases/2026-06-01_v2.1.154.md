# Реліз 2.1.154 — 2026-06-01

## Нові можливості
- Opus 4.8 — нова модель з default effort `high` (категорія: core)
- Fast mode на Opus 4.8 за 2x стандартної ціни з 2.5x швидкістю (категорія: core)
- Dynamic workflows — координація роботи через десятки/сотні агентів у фоні; `/workflows` для перегляду (категорія: skills-agents)
- `claude agents`: `! <command>` для запуску shell як background session; `claude --bg --exec` (категорія: cli)
- Lean system prompt за замовчуванням для всіх моделей крім Haiku, Sonnet, Opus 4.7 і старіших (категорія: core)
- Plugins `defaultEnabled: false` в `plugin.json` або marketplace entry (категорія: plugins)
- `/plugin` Discover tab pin-ить плагіни з "suggested for this directory" (категорія: plugins)
- Claude in Chrome browser picker через `/chrome` → "Select browser…" (категорія: vscode-ide)
- Streaming tool execution завжди увімкнено на всіх платформах (категорія: core, settings)
- `CLAUDE_CODE_SESSION_ID` та `CLAUDECODE=1` в stdio MCP subprocesses (категорія: mcp)
- `/claude-api` skill Opus 4.8 support та 4.7 → 4.8 migration guidance (категорія: sdk)

## Виправлення
- `rm -rf $HOME` не блокувався з trailing slash (core)
- `$TMPDIR` різні директорії в sandboxed vs unsandboxed Bash (core)
- Unreadable highlighted-row text в `claude agents` (core)
- Background agent notifications передчасне "out of context" (core)
- Background session classifier втрата цілі при scheduled command (core)
- Pinned background sessions respawn кожну хвилину після update (core)
- Background sessions stuck status не retire (core)
- Subagents background sessions обходять worktree-isolation (core)
- Orphaned `claude --bg-pty-host` 100% CPU на macOS (core)
- Number key shortcuts не працювали під divider (core)
- `worktree.baseRef: "head"` wrong HEAD в linked worktree (core)
- Stray leading space на wrapped lines (core)
- VS Code terminal rendering corruption від thinking spinner colors (core)
- Plan file names з `[Image #N]`/`[Pasted text #N]` placeholders (core)
- Phantom expand/click affordance на short ANSI-colored lines (core)
- Auto mode exfiltration detection покращено (core)
- `claude agents` `/logout` sign-out замість background (cli)
- `←←` agents view на Bedrock/Vertex/Foundry/telemetry-disabled (cli)
- `claude mcp list`/`get` pending approval замість auto-approve (cli)
- `/remote-control` autocomplete "Disconnect" (cli)
- Auto mode mode picker без bypass-permissions [VSCode] (vscode-ide)
- Task panel stray "main" row [VSCode] (vscode-ide)
- `/mcp` tools list rendering [VSCode] (vscode-ide)
- `/model` picker fast mode pricing [VSCode] (vscode-ide)
- Auto mode "could not evaluate" [VSCode] (vscode-ide)
- `allowedMcpServers`/`deniedMcpServers` invalid entry tolerance (settings)
- `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` deprecated (env-vars)
- `CLAUDE_CODE_ALWAYS_ENABLE_EFFORT` 400 на моделях без effort (env-vars)
- Windows update "exe in use" покращене повідомлення (windows)
- `/simplify` cleanup-only review замість `/code-review --fix` (skills-agents)

## Важливі зміни
- `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` застарілий — буде видалено 06/01
- `/effort` мітки слайдера перейменовані з "Speed"/"Intelligence" на "Faster"/"Smarter"
- `/simplify` більше не викликає `/code-review --fix`, натомість виконує cleanup-only review
- Claude теперAskUserQuestion лише для рішень що дійсно не може прийняти сам
