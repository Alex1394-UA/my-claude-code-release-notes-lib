# Реліз 2.1.113 — 2026-04-23

## Нові можливості
- Native binary spawn замість bundled JavaScript (категорія: cli, core)
- `sandbox.network.deniedDomains` setting (категорія: settings)
- Fullscreen `Shift+↑/↓` selection scroll (категорія: keybindings)
- `Ctrl+A`/`Ctrl+E` multiline input (категорія: keybindings)
- `Ctrl+Backspace` delete word [Win] (категорія: keybindings, windows)
- Long URLs clickable при wrapping (категорія: core)
- `/ultrareview` покращення — parallelized checks, diffstat, animated state (категорія: skills-agents)
- Subagent stall timeout — fail після 10 хв (категорія: core)

## Виправлення
- MCP concurrent-call timeout handling fix (mcp)
- `Cmd+Backspace` / `Ctrl+U` delete to start of line fix (keybindings)
- Markdown table broken with inline code pipe (core)
- Session recap auto-fire fix (core)
- `/copy` markdown table column alignment (core)
- Parent messages hidden in subagent transcript (core)
- `dangerouslyDisableSandbox` running outside sandbox (settings)
- `/effort auto` label fix (core)
- "Copied N chars" toast overcounting fix (core)
- `/insights` EBUSY crash [Win] (windows)
- Exit confirmation dialog one-shot vs recurring label (core)
- Slash/@ completion menu border in fullscreen (core)
- `CLAUDE_CODE_EXTRA_BODY` effort 400 errors (sdk)
- Prompt cursor NO_COLOR fix (core)
- `ToolSearch` ranking fix (core)
- Long context compact "Extra usage" fix (core)
- Plugin install range-conflict (plugins)
- "Refine with Ultraplan" missing remote session URL (core)
- SDK image content degrade to placeholder (sdk)
- RC subagent transcripts not streaming (sdk)
- RC sessions not archived on exit (sdk)
- Bedrock thinking disabled 400 error (sdk)
- `cd <current-dir> &&` no permission prompt (cli)
- Bash comment multiline UI-spoofing fix (core)
- macOS `/private/*` dangerous removal paths (settings)
- Bash deny exec wrappers match (settings)
- `Bash(find:*)` exec safety (settings)
