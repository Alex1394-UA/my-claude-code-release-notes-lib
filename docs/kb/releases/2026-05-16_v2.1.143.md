# Реліз 2.1.143 — 2026-05-16

## Нові можливості
- Plugin dependency enforcement: `claude plugin disable` відмовляє коли інший plugin залежить; `claude plugin enable` force-enables transitive dependencies (категорія: plugins)
- `/plugin` marketplace projected context cost — per-turn та per-invocation token estimates в browse pane (категорія: plugins)
- `worktree.bgIsolation: "none"` setting — background sessions можуть редагувати робочу копію без EnterWorktree (категорія: settings)
- PowerShell tool `-ExecutionPolicy Bypass` та default enable на Windows Bedrock/Vertex/Foundry (категорія: windows)
- `CLAUDE_CODE_POWERSHELL_RESPECT_EXECUTION_POLICY` для opt-out (категорія: env-vars)

## Виправлення
- Corrupt `.credentials.json` non-array `scopes` hanging CLI on startup (категорія: core)
- Right-click paste в `claude agents` на Windows Terminal/WSL (категорія: windows)
- Stop hooks що блокують повторно зациклювалися нескінченно — тепер 8 блоків cap (категорія: hooks, env-vars)
- Esc/Ctrl+C не скасовував pending `/loop` wakeup (категорія: core)
- `/goal` evaluator firing while background shells/subagents running (категорія: core)
- `NO_COLOR`/`FORCE_COLOR` в settings.json `env` stripping UI colors (категорія: settings)
- Agent view repeated PowerShell processes на Windows (категорія: windows)
- `/bg` без промпту відправляв "continue" до fork-сесії (категорія: core)
- `--agent <name>` не знаходив plugin-contributed agents без `plugin:` prefix (категорія: cli)
- Видалення сесії з agent view не видаляло transcript file (категорія: cli)
- Stale-fragment rendering в attached background sessions на Windows Terminal (категорія: windows)
- Background agents false-positive worker-stall detection після sleep/App Nap (категорія: core)
- 5xx error messages pointing at status.claude.com (категорія: core)
- ← в `claude agents` while streaming залишав list unresponsive на Windows (категорія: windows)
- Background sessions silently capturing IDE file references (категорія: core)
- Worktree cleanup `rm -rf` fallback loss prevention (категорія: core)
- macOS background sessions "Operation not permitted" для Documents/Desktop/Downloads (категорія: core)

## Покращення
- Background sessions preserve model/effort after idle wake (категорія: core)
- Shift+Tab в attached agent sessions тепер включає auto mode (категорія: core)
- `/bg` зберігає `--mcp-config`, `--settings`, `--add-dir`, `--plugin-dir`, `--strict-mcp-config` через respawn (категорія: core)
- `/bg`/←-detach preserve `--fallback-model` (категорія: core)
- `/bg`/←-detach preserve `--allow-dangerously-skip-permissions` (категорія: core)
- Background sessions honor `permissions.defaultMode` from settings.json (категорія: core)
- Background daemon spawn fallback до running binary коли launcher missing (категорія: core)

## Повторення з 2.1.142 (не додано до KB)
- `claude agents` `--add-dir`, `--settings`, `--mcp-config`, `--plugin-dir`, `--permission-mode`, `--model`, `--effort`, `--dangerously-skip-permissions`
- `claude --bg --dangerously-skip-permissions` persist across retire→wake
