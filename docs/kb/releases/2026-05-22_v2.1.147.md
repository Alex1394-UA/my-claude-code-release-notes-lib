# Реліз 2.1.147 — 2026-05-22

## Нові можливості
- Pinned background sessions (`Ctrl+T` в `claude agents`) — залишається живою при idle, рестартується in-place, shed-иться під memory pressure (категорія: cli, skills-agents)
- `/code-review` (замість `/simplify`) — code review з рівнем зусилля (`/code-review high`), `--comment` для GitHub PR comments; стара cleanup-and-fix поведінка видалена (категорія: skills-agents)
- Auto-updater retries transient network failures, specific error categories, OS error codes (категорія: core)
- Diff rendering performance для великих файлових редагувань (категорія: core)
- Prompt history не записує послідовні дублікати (категорія: core)

## Виправлення
- Enterprise login restrictions (`forceLoginOrgUUID`, `forceLoginMethod`) не застосовувались до 3p/API-key sessions (категорія: settings)
- `&` в `!` command output що відображався як `&amp;` (категорія: core)
- Unknown slash commands мовчазно ігнорувались в headless/SDK (категорія: core, sdk)
- `/help` зламаний tab header та одна команда на сторінці (категорія: core)
- Shell snapshot dropping user functions з underscore (категорія: core)
- Plugin agents з multiple `Agent(...)` types dropping entries (категорія: plugins)
- Hook `if` conditions `PowerShell(git push*)` ніколи не збігалися (категорія: hooks)
- PowerShell tool dropping output для default formatter commands (категорія: core)
- "Yes, and don't ask again" PowerShell permission rules не збігалися (категорія: windows)
- PowerShell tool exit code 1 з winget/Store pwsh (категорія: windows)
- `/effort` відкривався зі слайдером на неправильному рівні (категорія: core)
- Paginating MCP servers dropping resources/templates/prompts past page 1 (категорія: mcp)
- Full-screen strobing attached background sessions Windows Terminal (категорія: windows)
- Background-job worktree removal following NTFS junctions (категорія: windows)
- `/background` відмовлявся від sessions з лише skill/slash command input (категорія: core)
- Auto mode suppressing `AskUserQuestion` (категорія: core)
- `/theme` Esc не працював в "New custom theme" та color editor (категорія: core)
- Uncaught exception в кінці streaming sessions Agent SDK (категорія: sdk)
- Rare hang scroll settle на Windows (категорія: windows)
- Stale/doubled rows agent view Windows CJK characters (категорія: windows)
- Pasted text як `[Pasted text #N]` placeholder (категорія: core)
- Plugin component counts doubled `claude plugin details` (категорія: plugins)
- Backgrounded sessions re-prompting для вже наданих permissions (категорія: core)
- GNOME Terminal right-click/middle-click paste (категорія: core)
- `CLAUDE_CODE_SUBAGENT_MODEL` не застосовувався до teammate processes (категорія: env-vars, skills-agents)
- Slash commands з tab/newline як unknown command (категорія: core)
- Spacing/layout в `/plugin`, `/status`, `/mobile`, `/sandbox`, `/permissions` menus (категорія: core)
- Stripped images prompting re-read absent media (категорія: core)

## Важливі зміни
- `/simplify` перейменовано на `/code-review` — стара cleanup-and-fix поведінка **видалена**, нова команда робить code review з effort level та `--comment` для GitHub PR comments
