# Реліз 2.1.126 — 2026-05-04

## Нові можливості
- `/model` picker показує моделі з gateway `/v1/models` endpoint (категорія: core)
- `claude project purge [path]` — видалення стану проекту (категорія: cli)
- `--dangerously-skip-permissions` розширено на `.claude/`, `.git/`, `.vscode/`, shell config (категорія: cli, settings)
- `claude auth login` приймає OAuth code в терміналі (категорія: cli)
- `claude_code.skill_activated` OTEL event з `invocation_trigger` (категорія: env-vars, skills-agents)
- Auto mode спіннер червоніє при permission stall (категорія: core)
- Host-managed deployments більше не auto-disable analytics (категорія: settings)
- Windows: PowerShell 7 detection expansion (категорія: windows)
- Windows: PowerShell як primary shell коли tool увімкнено (категорія: windows)
- Read tool: прибрано malware-assessment reminder (категорія: core)

## Виправлення
- **Безпека:** `allowManagedDomainsOnly`/`allowManagedReadPathsOnly` ігнорувалися (категорія: settings)
- Oversized image paste ломав сесію (категорія: core)
- "OAuth not allowed" login screen замість guidance (категорія: cli)
- OAuth login timeout на повільних/proxied з'єднаннях (категорія: cli)
- Concurrent credential write race (категорія: cli)
- API retry countdown stuck на "0s" (категорія: core)
- "Stream idle timeout" після Mac wake (категорія: core)
- Background/remote sessions false "Stream idle timeout" (категорія: sdk)
- Порожній output після thinking (категорія: core)
- Fast trackpad scrolling в Cursor та VS Code 1.92–1.104 (категорія: vscode-ide)
- claude.ai MCP connectors suppressed by manual servers (категорія: mcp)
- Japanese/Korean/Chinese garbled text на Windows (категорія: windows)
- `Ctrl+L` clearing prompt input (категорія: keybindings)
- Deferred tools unavailable для `context: fork` skills (категорія: skills-agents)
- Plan-mode tools unavailable з `--channels` (категорія: core)
- `/plugin` Uninstall показував "Enabled" (категорія: plugins)
- File-modified reminders unbounded (категорія: core)
- `/remote-control` retries stuck on "connecting…" (категорія: sdk)
- RC failure notification без error reason (категорія: sdk)
- Windows clipboard EDR exposure та >22KB (категорія: windows)
- PowerShell bare `--` mis-flagged як stop-parsing (категорія: windows)
- Agent SDK hang malformed tool name в parallel batch (категорія: sdk)

## Важливі зміни
- `--dangerously-skip-permissions` значно розширено — тепер bypass для `.claude/`, `.git/`, `.vscode/`, shell config
