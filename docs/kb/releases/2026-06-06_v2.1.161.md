# Реліз 2.1.161 — 2026-06-06

## Нові можливості

- `OTEL_RESOURCE_ATTRIBUTES` значення тепер включаються як labels на metric datapoints для кастомних dimensions (team, repo)
- `claude agents` рядки показують `done/total` при fan-out; peek показує найдовший елемент
- `/mcp` згортає unused claude.ai connectors за рядок "Show unused connectors"
- Fullscreen clipboard на Linux тепер використовує `wl-copy`/`xclip`/`xsel`, копіює в clipboard та PRIMARY selection

## Виправлення

- Parallel tool calls: failed Bash command більше не скасовує інші виклики в тому ж batch
- `/effort` dialog, workflow animations та prompt keyword shimmer не поважали "Reduce motion" setting
- `forceLoginOrgUUID`/`forceLoginMethod` блокували 3p provider sessions разом з org pin (регресія 2.1.146)
- Background subagent output corrupting `claude -p` stdout з `--output-format text`/`json`
- `/usage-credits` запускав re-login замість посилання на org usage settings
- `/autofix-pr` повідомляв "cannot run on the default branch" в worktree
- `--resume` picker не показував сесії з поточної директорії в non-git worktree (jj workspaces)
- Windows hooks з явним викликом bash failing з "command not found"
- OTEL log events мовчазно відкидались до telemetry initialization
- `claude mcp` list/get/add друкували secrets: `${VAR}` та credential headers/URL secrets замасковані
- Workflow agents з `isolation: "worktree"` в background sessions заблоковані від редагування власного worktree
- Background sessions з `claude agents` завантажували застарілу модель з daemon
- Краш при рендерингу Write tool results після відновлення сесії
- Завершені субагенти застрягали в стані "running" при помилці фіналізації
- `EADDRINUSE` при deep `CLAUDE_CODE_TMPDIR` шляху
- [VSCode] Додано підказку про вимкнення GPU acceleration для garbled glyphs

## Покращення

- Стабілізація JIT compilation profile layout engine — краща продуктивність рендерингу
- Покращено рендеринг великих файлів
