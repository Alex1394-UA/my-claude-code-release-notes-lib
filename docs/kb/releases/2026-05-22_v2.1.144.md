# Реліз 2.1.144 — 2026-05-22

## Нові можливості
- `/resume` для background sessions — sessions з `claude --bg` та agent view тепер appear в picker, позначені `bg` (категорія: cli)
- Elapsed duration в background subagent completion notifications (категорія: core)
- `/plugin` browse та discover panes показують last-updated дату плагіна (категорія: plugins)
- `/model` тепер змінює модель лише для поточної сесії; `d` для default (категорія: cli, core)
- `/usage-credits` як нова назва замість `/extra-usage` (категорія: skills-agents)
- `claude --bg --name <label>` підтверджує ім'я в post-spawn повідомленні (категорія: cli)
- Post-survey follow-up hint з context-aware copy (категорія: core)

## Виправлення
- Startup hanging до 75s коли API недоступний — side-channel calls timeout 15s (категорія: core)
- Garbled terminal output після пропущеного window-resize — self-heals (категорія: core)
- Progressive display corruption в довгих сесіях (категорія: core)
- VS Code spinner animation color reduction для зменшення glitches (категорія: core, vscode-ide)
- macOS background sessions crash з "exit 1 before init" в Full Disk Access folders (регресія 2.1.143) (категорія: core)
- Unreadable image extension fallback до text (категорія: core)
- Spurious tool errors: `head`/`tail` satisfy read-before-edit, exit 1 від egrep/fgrep/git grep/git diff не report як failure (категорія: core)
- AskUserQuestion Escape в notes field abort turn (категорія: core)
- Model selection не застосовувався через IDE picker / applyFlagSettings (категорія: core)
- Resumed sessions підхоплювали чужу `/model` настройку (категорія: core)
- Bedrock/Vertex не могли обрати Opus 1M context (регресія v2.1.129) (категорія: cli)
- Remote-session login fail з `forceLoginMethod`/`forceLoginOrgUUID` (категорія: cli)
- MCP paginated `tools/list` лише перша сторінка (категорія: mcp)
- MCP images з unsupported MIME (SVG) break conversation (категорія: mcp)
- File descriptor exhaustion в skill directory builds (категорія: hooks)
- Session title з plugin monitor output (категорія: skills-agents)
- Skill tool permission error в headless mode (регресія v2.1.141) (категорія: skills-agents)
- Plugin "not cached" errors на fresh machine (категорія: plugins)
- `claude mcp list` silent при malformed `.mcp.json` (категорія: cli)
- Background side-queries custom BASE_URL / Bedrock Mantle fallback (категорія: cli)
- Windows: scrolling attached background sessions (категорія: windows)
- Windows: crash при закритті терміналу з attached background session (категорія: windows)
- Windows: `claude agents` ← unresponsive list (категорія: windows)
- Windows: ghost characters CJK Agent View (категорія: windows)
- `/bg`/←-detach preserve `/add-dir` (категорія: cli)
- Edit/Write background session isolation false-positive (категорія: cli)
- `claude respawn` stopped session showing "stopped" (категорія: cli)
- `/resume` picker не показував forked background sessions (категорія: windows)
- `claude agents`/`claude logs` hang при unresponsive daemon — 10s timeout (категорія: cli)
- Background Bash tasks SDK panel "Running" stale (категорія: cli)
- Completed/stopped sessions wake permanently marked crash (категорія: cli)
- Markdown links в `claude agents` plain text (категорія: windows)
- `spinnerVerbs` applying до post-turn duration (категорія: settings)
- `/branch` "No conversation to branch" в worktree/background (категорія: cli)
- `/plugin` return to Installed tab after action (категорія: plugins)
- `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` для marketplace (категорія: plugins)
- Background session worktree VCS guard для non-git (категорія: cli)
- Skill-listing truncation не як startup notification (категорія: cli)
- `/doctor` exec-form hook hint (категорія: cli)
- Pre-response stream stall retry improvement (категорія: core)
- SDK/headless MCP startup overlap (до 2s швидше) (категорія: core)
