# Реліз 2.1.105 — 2026-04-16

## Нові можливості
- `path` параметр для `EnterWorktree` — перехід в існуючий worktree (категорія: cli)
- PreCompact хуки можуть блокувати compact через exit code 2 або `{"decision":"block"}` (категорія: hooks)
- Background monitors для плагінів через `monitors` manifest key (категорія: plugins)
- `/proactive` аліас для `/loop` (категорія: skills-agents)
- Покращено описи skill: ліміт listing піднято з 250 до 1536 символів з попередженням при обрізанні (категорія: skills-agents)

## Покращення
- API стрімінг автоматично abort через 5 хв без даних, retry non-streaming (категорія: core)
- Повідомлення про помилку з'єднання показують retry негайно (категорія: core)
- Long single-line writes обрізаються в UI замість пагінації (категорія: core)
- `/doctor` layout зі статус-іконками, `f` для автовиправлення (категорія: core)
- `/config` покращені labels та описи (категорія: settings)
- `WebFetch` прибирає `<style>` та `<script>` (категорія: core)
- Очищення worktrees чий PR squash-merged (категорія: cli)
- MCP large-output truncation з формат-специфічними рецептами (категорія: mcp)

## Виправлення
- Втрата зображень в черзі повідомлень (core)
- Порожній екран при wrap prompt input (core)
- Копіювання whitespace при виділенні в fullscreen (core)
- Обрізання whitespace з відповідей (ASCII art) (core)
- Пошкоджений bash output з клікабельними links (core)
- Зникнення промптів з черги в focus mode (core)
- Повторне спрацьовування one-shot scheduled tasks (core)
- Feedback survey shortcut keys (core)
- Дублювання "Creating worktree" тексту (cli)
- Відсутність "Resume this session" hint (cli)
- Inbound channel notifications для Team/Enterprise (mcp)
- Marketplace dependencies не встановлюються (plugins)
- Marketplace auto-update ламає marketplace (plugins)
- Alt+Enter newline в ESC-prefix терміналах + Ctrl+J регресія (keybindings)
- /model picker Bedrock non-US persisting невалідних IDs (core)
- 429 rate-limit показує JSON dump (core)
- Краш при resume malformed text blocks (cli)
- /help випадає на коротких терміналах (core)
- Malformed keybindings.json values (settings)
- CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC впливає на всі проекти (env-vars)
- Бліда 16-кольорова палітра через SSH/mosh (core)
- Bash tool пропонує acceptEdits при виході з план-режиму (core)
