# Реліз 2.1.92 — 2026-04-06

## Нові можливості
- `forceRemoteSettingsRefresh` policy setting (fail-closed remote managed settings) (категорія: settings)
- Інтерактивний Bedrock setup wizard з екрану логіну (категорія: cli)
- `/cost` з per-model та cache-hit breakdown для subscription користувачів (категорія: core)
- `/release-notes` — інтерактивний вибір версії (категорія: core)
- Remote Control імена сесій з hostname prefix + `--remote-control-session-name-prefix` (категорія: sdk)
- Footer hint при поверненні до сесії після спливання prompt cache (категорія: core)

## Виправлення
- Subagent spawn збій після kill/renumber tmux windows (категорія: core)
- Prompt-type Stop hooks помилково при `ok:false` від fast model; `preventContinuation:true` відновлено (категорія: hooks)
- Tool input валідація при streaming array/object як JSON strings (категорія: core)
- API 400 при whitespace-only text block в extended thinking (категорія: core)
- Випадкові feedback survey submissions від auto-pilot keypresses (категорія: core)
- Оманливий "esc to interrupt" hint при text selection в fullscreen (категорія: core)
- Homebrew update prompts використовують release channel cask (категорія: cli)
- `ctrl+e` перехід на кінець наступного рядка в багаторядковому промпті (категорія: core)
- Дублювання повідомлення при скролі вверх (DEC 2026 термінали) (категорія: core)
- Idle-return "/clear to save" показував кумулятивні замість поточних токенів (категорія: core)
- Plugin MCP servers зависали "connecting" при дублюванні неавтентифікованого конектора (категорія: mcp)

## Продуктивність
- Write tool diff computation прискорено для великих файлів (+60% на tabs/`&`/`$`) (категорія: core)

## Важливі зміни
- `/tag` — ВИДАЛЕНО (категорія: skills-agents)
- `/vim` — ВИДАЛЕНО, використовуйте `/config` → Editor mode (категорія: skills-agents)
- Linux sandbox `apply-seccomp` helper тепер в npm та native builds (категорія: settings)
