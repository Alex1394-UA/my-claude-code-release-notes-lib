# Реліз 2.1.118 — 2026-04-23

## Нові можливості
- Vim visual mode `v` та visual-line mode `V` (категорія: keybindings)
- Merged `/cost` + `/stats` → `/usage` (категорія: core)
- Custom themes з `/theme` та `~/.claude/themes/` (категорія: core, settings)
- Hooks `type: "mcp_tool"` для MCP tool calls (категорія: hooks)
- `DISABLE_UPDATES` env var (категорія: env-vars)
- WSL `wslInheritsWindowsSettings` policy (категорія: settings)
- Auto mode `"$defaults"` include (категорія: core, settings)
- "Don't ask again" для auto mode opt-in (категорія: core)
- `claude plugin tag` для release git tags (категорія: cli)
- `--continue`/`--resume` з `/add-dir` сесіями (категорія: cli)
- `/color` sync до claude.ai/code з Remote Control (категорія: core)
- `/model` honors `ANTHROPIC_DEFAULT_*_MODEL` з custom BASE_URL (категорія: core)
- Plugin auto-update skip в `/doctor` та Errors tab (категорія: plugins)

## Виправлення
- MCP OAuth `headersHelper` menu fix (mcp)
- MCP HTTP/SSE custom headers 401 hang (mcp)
- OAuth `expires_in` missing — hourly re-auth fix (mcp)
- OAuth step-up silent refresh (mcp)
- OAuth timeout/cancel unhandled rejection (mcp)
- OAuth refresh cross-process lock (mcp)
- macOS keychain race (mcp)
- OAuth token early revocation (mcp)
- Credential save crash Linux/Windows (core)
- `/login` no effect з env token — clears env token (sdk)
- Unreadable text badges/pill (core)
- Plan auto mode vs bypass permissions label (core)
- Agent-type hooks messages required fix (hooks)
- Prompt hooks re-fire on agent-hook verifier (hooks)
- `/fork` full parent write → pointer (cli)
- Remote session overwrite local model (core)
- Typeahead `/` path pasting error (core)
- Plugin install wrong version re-resolve (plugins)
- File watcher errors on invalid paths (core)
- RC archived on JWT refresh blip (sdk)
- Subagent resumed `cwd` fix (skills-agents)
- `Alt+K`/`Alt+X`/`Alt+^`/`Alt+_` freeze (keybindings)
