# Реліз 2.1.153 — 2026-05-28

## Нові можливості
- Status line команди отримують `COLUMNS` та `LINES` змінні середовища (категорія: core)
- macOS background agents appear як "Claude Code" в Privacy & Security та зберігають permission grants через оновлення (категорія: core)
- `/model` тепер зберігає вибір як default для нових сесій; `s` для session-only switch (категорії: core, keybindings)
- Об'єднано "needs authentication" startup notifications для MCP servers та connectors (категорія: mcp)
- `skipLfs` опція для `github`/`git` plugin marketplace sources (категорія: plugins)
- `claude agents` autocomplete пропонує native slash команди та bundled skills (категорії: cli, skills-agents)
- `claude agents` PR column показує `PR #N` / `N PRs` (категорія: cli)
- `claude doctor` показує результат останньої спроби оновлення (категорія: core, cli)
- One-time notice коли npm global install не може auto-update; `/doctor` показує fixes (категорія: core)
- `--strict-mcp-config` більше не strip-ає inline `mcpServers` з explicit agent definitions; blocked servers показують warning (категорія: cli, settings)

## Виправлення
- Stateful MCP servers без GET SSE stream reconnect-loop на `tools/list` (регресія з v2.1.147) (категорія: mcp)
- Custom API gateway отримував user OAuth credential замість gateway token (категорія: mcp)
- Subagent frontmatter MCP servers ігнорували `--strict-mcp-config` та інші policies (категорії: mcp, sdk)
- MCP tool progress notifications не рендерились в collapsed view (категорія: mcp)
- PowerShell installer хибно повідомляв "Installation complete!" на Windows (категорія: windows)
- `claude update` встановлював latest замість configured release channel для npm (категорії: cli, windows)
- Excessive memory (multi-GB) при resume по transcript path (категорія: core)
- `claude agents`/`--bg` на stale daemon до binary-takeover support (категорія: cli)
- Hang при stdin closed без EOF в stream-json mode (категорія: core)
- Malformed `file://` links не клікабельні в терміналі (категорія: core)
- `claude --help` нерозбитий на вузьких терміналах (<92 cols) (категорія: cli)
- `Agent` tool `subagent_type: 'claude'` в temporary worktree міг discard gitignored outputs (категорія: core)
- `/bg` під час відповіді тепер продовжує response замість drop (категорія: core)
- `/btw` keyboard shortcuts unresponsive в background sessions (категорія: core)
- Background sessions temp files в `$CLAUDE_JOB_DIR` triggering sensitive file prompt (категорія: core)
- Background agent deleted cwd showing truncated stack trace (категорія: core)
- `EnterWorktree` не доступний одразу в background sessions (категорія: core)
- `cmd+k` iTerm2/Terminal.app не перемальовував attached background sessions (категорія: core)
- IME candidate window position fix на Windows (категорія: windows)
- Background-color bleed з 256-color terminals при attaching (категорія: core)
- `/copy` clipboard fail в tmux background sessions (категорія: core)
- `claude agents` з Remote Control залишав zombie session entries (категорія: sdk)
- `/rename` в background sessions не оновлював banner (категорія: core)
- Windows update rollback restore (категорія: windows)
- VS Code processes не shutdown cleanly на Windows (категорії: vscode-ide, windows)

## Важливі зміни
- `modelPicker:setAsDefault` keybinding renamed на `modelPicker:thisSessionOnly` (action `d` replaced by `s`) (категорія: keybindings)
