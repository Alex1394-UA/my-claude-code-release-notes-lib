# Реліз 2.1.119 — 2026-04-25

## Нові можливості
- `/config` settings persist в `~/.claude/settings.json` з правильним precedence (settings)
- `prUrlTemplate` налаштування для кастомного PR badge URL (settings)
- `CLAUDE_CODE_HIDE_CWD` змінна середовища для приховання CWD в лого (env-vars)
- `--from-pr` підтримує GitLab merge-request, Bitbucket pull-request, GitHub Enterprise (cli)
- `--print` mode враховує `tools:` та `disallowedTools:` з agent frontmatter (cli)
- `--agent <name>` поважає `permissionMode` для built-in агентів (cli)
- PowerShell auto-approve в permission mode (settings)
- `PostToolUse`/`PostToolUseFailure` hook inputs включають `duration_ms` (hooks)
- Subagent/SDK MCP сервери підключаються паралельно (mcp, sdk)
- Плагіни закріплені version constraint auto-update до найвищого satisfying tag (plugins)
- `blockedMarketplaces` enforce `hostPattern` та `pathPattern` (plugins)
- OTEL `tool_result`/`tool_decision` включають `tool_use_id` та `tool_input_size_bytes` (env-vars)
- Status line stdin JSON включає `effort.level` та `thinking.enabled` (env-vars)
- Vim Esc в INSERT не підтягує повідомлення з черги (core, keybindings)
- Slash command suggestions підсвічують символи що збіглися (core)
- Slash command picker переносить довгі описи (core)
- `owner/repo#N` використовує git remote host (core)

## Виправлення
- CRLF paste вставляла зайвий пустий рядок (windows)
- Multi-line paste втрата newlines в kitty keyboard protocol (core)
- Glob/Grep зникання коли Bash tool заблокований (windows, core)
- Fullscreen scroll snap-back при завершенні tool (core)
- Rewind overlay "(no prompt)" для image attachments (core)
- Auto mode перезаписував plan mode (core)
- Async PostToolUse писав пусті записи в транскрипт (core)
- Subagent orphan spinner зависання (core)
- Tool search вимкнено на Vertex AI (mcp)
- `@`-file Tab completion замінював весь prompt (core)
- `${ENV_VAR}` не підставлялись в MCP headers (mcp)
- OAuth client_secret_post не відправлявся (mcp)
- Plugin MCP падав на пустих `${user_config.*}` (mcp)
- Plugin MCP сервери не з'являлись на Windows (windows)
- MCP "cmd /c" false-positive warning (windows)
- `/skills` Enter закривав замість pre-fill (core)
- `/agents` detail view "Unrecognized" для built-in tools (core)
- `/export` показував дефолтну модель (core)
- Verbose output не зберігався після рестарту (core)
- `/usage` progress bars перекривали labels (core)
- List item number wrapping (core)
- `/plan`/`/plan open` не діяли на існуючий план (core)
- Skills re-execution після auto-compaction (core)
- Agent worktree stale reuse (core)
- Disabled MCP показувався як "failed" (core)
- TaskList sort по ID (core)
- Spurious GitHub rate limit hints (core)
- SDK bridge `read_file` size cap (sdk)
- PR не прив'язувався в worktree (core)
- `/doctor` false warning про overridden MCP (core)
- Voice dictation пустий recording на macOS (vscode-ide)
- `/reload-plugins`/`/doctor` звіти про disabled plugins (core)
- Stray `p` character на startup в macOS Terminal (core)

## Важливі зміни
- Немає депрекацій
