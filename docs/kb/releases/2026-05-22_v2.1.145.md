# Реліз 2.1.145 — 2026-05-22

## Нові можливості
- `claude agents --json` — CLI вивід live sessions як JSON для скриптів (категорія: cli)
- `agent_id` та `parent_agent_id` OTEL spans з correct trace parenting (категорія: sdk)
- Status line JSON input з GitHub repo та PR info (категорія: sdk)
- `/plugin` Discover/Browse показують commands, agents, skills, hooks, MCP/LSP перед встановленням (категорія: plugins)
- `claude agents` tab title показує awaiting-input count (категорія: cli)
- Slash command / @-mention mouse hover та click в fullscreen mode (категорія: core)
- Stop/SubagentStop hook input `background_tasks` та `session_crons` fields (категорія: hooks)
- Read tool truncated "PARTIAL view" замість hard error при великому файлі (категорія: core)

## Виправлення
- Permission-prompt bypass bare variable assignments (категорія: core)
- MCP prompt slash commands raw server validation errors (категорія: mcp)
- Spinner/elapsed-time freeze після resize/refocus (категорія: core)
- Cross-project resume hint PowerShell 5.1 (категорія: windows)
- Voice push-to-talk не працював в agent view reply pane (категорія: core)
- Task lists random order при створенні кількох одночасно (категорія: core)
- Stale "Failed to install Anthropic marketplace" banner (категорія: plugins)
- PR badge в footer не оновлювався після `gh pr create` (категорія: core)
- Agent Teams non-ASCII names failing API calls через invalid header encoding (категорія: core)
- `/review` deprecated `projectCards` GraphQL query (категорія: cli)
- `claude plugin validate` не flag-ив `skills:` що point на file замість directory (категорія: plugins)
- `context: fork` skill infinite loop (категорія: skills-agents)
