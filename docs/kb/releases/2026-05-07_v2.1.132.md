# Реліз 2.1.132 — 2026-05-07

## Нові можливості
- `CLAUDE_CODE_SESSION_ID` env var в subprocesses Bash tool (env-vars)
- `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` для opt-out від fullscreen alternate-screen renderer (env-vars)
- "Pasting…" footer hint під час Ctrl+V image paste (core)

## Виправлення
- External SIGINT (IDE stop, `kill -INT`) тепер graceful shutdown з terminal restore та `--resume` hint (core)
- Uncaught exception при закритті терміналу або SSH disconnect під native build (core)
- `--resume` crash `no low surrogate in string` при emoji truncation; pre-corrupted sessions sanitize on load (core)
- `--permission-mode` flag ігнорувався при resume plan-mode session (core)
- Fullscreen blank screen після laptop sleep/wake або Ctrl+Z/`fg` (core)
- Cursor mid-grapheme на Indic conjunct або ZWJ emoji wrap (core)
- Vim operators corrupting decomposed (NFD) accented characters (core)
- Paste text з `/` мовчазно поглиналось або trigerra unknown-command (core)
- Paste escape sequences при interleaved focus events/mouse-tracking (core)
- Mouse wheel надто швидкий в Cursor та VS Code 1.92–1.104 (vscode-ide)
- Scroll-wheel handling в JetBrains IDE 2025.2 terminals (vscode-ide)
- `/usage` Ctrl+S hanging при копіюванні screenshot на Linux/X11 (core)
- `/terminal-setup` суперечливе повідомлення в Windows Terminal (core)
- `/effort` picker не відображав `CLAUDE_CODE_EFFORT_LEVEL` override (core)
- `/status` wrong default model (core)
- Slash command autocomplete обмежений ~3–5 команд замість масштабування (core)
- Statusline `context_window` відображав кумулятивні підсумки замість поточного використання (core)
- Alt+T thinking toggle не працював на macOS без "Option as Meta" (keybindings)
- Dead keyboard input на Windows при повторному відкритті фоновой сесії (windows)
- Unbounded memory growth (10GB+ RSS) при non-protocol MCP server stdout (mcp)
- MCP сервери з failed `tools/list` показують "connected · tools fetch failed" замість 0 tools (mcp)
- Unauthorized claude.ai MCP connectors показують "needs auth" замість "failed" (mcp)
- Bedrock та Vertex 400 помилки з `ENABLE_PROMPT_CACHING_1H` (mcp, env-vars)
- Visual consistency improvements в dialogs та `/login` spacing (core)
- `/tui fullscreen` banner з додатковими renderer benefits (core)
