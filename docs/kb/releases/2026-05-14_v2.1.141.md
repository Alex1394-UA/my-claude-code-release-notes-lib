# Реліз 2.1.141 — 2026-05-14

## Нові можливості
- Hook `terminalSequence` output field — desktop notifications, window titles, bells (категорія: hooks)
- `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` — HTTPS замість SSH для plugin clones (категорія: env-vars)
- `ANTHROPIC_WORKSPACE_ID` — workload identity federation scope (категорія: env-vars)
- `claude agents --cwd <path>` — scope session list (категорія: cli, skills-agents)
- `/feedback` recent sessions — останні 24h або 7 днів (категорія: core)
- Rewind menu "Summarize up to here" (категорія: core)
- IDE diff view на permission prompts відновлено (категорія: core, vscode-ide)
- Background agents preserve permission mode (категорія: core)
- Spinner amber 10s signal (категорія: core)
- Plugin menu navigation `→`/Tab/`↑` (категорія: plugins)
- [VSCode] Mic silence feedback "No audio detected" (категорія: vscode-ide)

## Виправлення
~50 fixes across 12 categories. Див. повний снапшот для деталей.

## Важливі зміни
- Bedrock `awsCredentialExport` тепер always runs коли configured (фіксить cross-account access)
