# Реліз 2.1.140 — 2026-05-13

## Виправлення
- Agent tool `subagent_type` matching case/separator-insensitive (категорія: skills-agents)
- `/goal` silent hang з `disableAllHooks`/`allowManagedHooksOnly` (категорія: core, settings)
- Settings hot-reload symlink regression — spurious `ConfigChange` hooks (категорія: settings, hooks)
- `claude --bg` connection drop при idle-exit (категорія: cli, sdk)
- Background service startup enterprise endpoint security (категорія: cli, sdk)
- Remote managed settings 401 retry з force-refresh (категорія: settings, sdk)
- `extraKnownMarketplaces` auto-update persistence (категорія: settings, plugins)
- `/loop` redundant wakeups для background tasks (категорія: core)
- Event-loop stall на Windows з missing executable (категорія: windows, core)
- `Read` tool offset whitespace/`+`-prefix validation (категорія: core)
- Terminal cursor focus loss (категорія: core)
- Plugin default component folder warning (категорія: plugins)
