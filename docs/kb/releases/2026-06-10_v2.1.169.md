# Реліз 2.1.169 — 2026-06-10

## Нові можливості
- `post-session` lifecycle hook для self-hosted runner (категорія: hooks, sdk)
- `--safe-mode` flag + `CLAUDE_CODE_SAFE_MODE` — запуск без кастомізацій для трублшутингу (категорія: cli, env-vars)
- `/cd` — перехід сесії в нову робочу директорію (категорія: cli)
- `disableBundledSkills` setting + `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS` (категорія: settings, env-vars)
- `API_FORCE_IDLE_TIMEOUT` — відновлено 5-хв idle timeout на Vertex/Foundry (категорія: env-vars)
- Self-hosted runner configurable SIGTERM→SIGKILL window (категорія: sdk)

## Виправлення
- Up/Down arrows jumping past wrapped rows (категорія: keybindings)
- Enterprise MCP policies not enforced on reconnect/first session (категорія: settings)
- ~30-50ms UI stall на macOS з claude.ai credentials (категорія: core)
- `claude -p` slow/hanging на Windows slash-command scan (категорія: cli, windows)
- Remote Control stuck "reconnecting" + OAuth refresh race (категорія: core)
- Git Credential Manager popup на Windows startup (категорія: windows)
- Footer hints not showing з custom statusline (категорія: core)
- Stale permission prompts on remote reattach (категорія: core)
- `claude agents --json` omitting blocked/dispatched sessions (категорія: cli)
- Agents view stale frame WSL/Windows Terminal (категорія: windows)
- Background agents ignoring project-level `env` (категорія: core)
- MCPB plugin cache spurious invalidation (категорія: windows)
- Plugin `.in_use` PID lock files accumulating (категорія: plugins)
- Untrusted settings OTEL client-cert paths (категорія: settings)
- `/workflows` відкривається під час turn (категорія: cli)
- `TaskCreate` reliability improvements (категорія: core)
- Improved error message for org disabled API key auth (категорія: core)
- Reduced CPU usage during streaming/spinner (категорія: core)
- Remote-managed settings invalid entry applies remaining policies (категорія: settings)
- Background sessions preserve flags across retire→wake (категорія: core)
- Background sessions told shared-checkout edits blocked (категорія: core)
- "CLAUDE.md is too long" scales with model context window (категорія: core)
- Auto-updater Windows retry held process (категорія: windows)
- Improved color contrast skill tags (категорія: core)
- Promo credit claims guidance (категорія: core)
- Tip: `claude agents` for concurrent sessions (категорія: core)

## Важливі зміни
- Немає
