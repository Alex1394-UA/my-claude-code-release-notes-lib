# Реліз 2.1.172 — 2026-06-11

## Нові можливості
- Субагенти тепер можуть spawnити власних субагентів (до 5 рівнів вкладеності)
- Пошуковий рядок при перегляді плагінів marketplace в `/plugin`
- OTEL metric `claude_code.lines_of_code.count` тепер містить атрибут `model`
- Amazon Bedrock читає AWS region з `~/.aws` config; `/status` показує джерело region

## Виправлення
- Сесії з 1M context без usage credits більше не застрягають назавжди
- Повторювана помилка обробки зображень в розмовах з кількома images
- `availableModels` fix: приховані 1M model rows, restrictions не застосовувались до subagent overrides
- `/model` picker пропонував недоступні моделі на Bedrock
- Подвоєння 1M suffix в model IDs
- `opusplan` не мав 1M context в plan mode
- `WebFetch` wildcard domain rules та file permission rules з mid-pattern wildcards
- Agents view busy spinner, background agents wrong settings, EAUTH attach, stuck nested agents
- Up-arrow prompt history показував промпти основного агента
- Workflow validation помилково відхиляла скрипти з згадкою `Date.now()`/`Math.random()`
- Performance improvements в довгих розмовах, зменшення idle CPU
- Chrome tool batched loading
- Remote Control footer shortened
- `/code-review` ultra option visible без claude.ai
- PowerShell tool calls rendering в VS Code

## Важливі зміни
- Mouse tracking вимкнено на Windows consoles з неповною підтримкою
