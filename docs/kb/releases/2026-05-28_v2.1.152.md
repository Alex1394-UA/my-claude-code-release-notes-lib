# Реліз 2.1.152 — 2026-05-28

## Нові можливості

- `/code-review --fix` — автоматичне застосування review знахідок до working tree
- `/simplify` тепер invoke-є `/code-review --fix`
- Skills/slash commands frontmatter `disallowed-tools` для видалення tools під час активного skill
- `/reload-skills` — перезавантаження skill directories без restart
- `SessionStart` hook `reloadSkills: true` — re-scan skill directories, skills доступні в тій самій сесії
- `SessionStart` hook session title через `hookSpecificOutput.sessionTitle` на startup/resume
- `MessageDisplay` hook event — трансформація/приховування тексту assistant message
- `pluginSuggestionMarketplaces` managed setting — admin allowlist для org marketplace plugin suggestions
- `claude plugin marketplace remove --scope user|project|local`
- Auto mode більше не потребує opt-in consent
- Vim mode `/` reverse history search в NORMAL mode
- Thinking summaries readability improvements (3s, markdown, 10 lines, Ctrl+O)
- Fullscreen thinking live counter
- Workflow tool inline progress simplified
- Post-response timer з background agents/workflows
- OTEL metric `app.entrypoint` (opt-in `OTEL_METRICS_INCLUDE_ENTRYPOINT=true`)
- `--fallback-model` тепер switch-иться для всієї сесії коли primary model not found

## Виправлення

- Terminal styling в довгих сесіях (renderer style pool recycled)
- Sandbox-enabled warning в condensed startup
- Thinking spinner "still thinking" під час tool running
- Focus mode spurious "N messages hidden"
- Link click в expanded tool result collapsing section
- Markdown table cell borders, continuation lines, empty header cells
- Effort-change confirmation dialog
- Agent tool description з `--bare`
- Stale thinking-block signatures після model/login switch
- `/usage` large session files (streaming read)
- `/doctor` stale marketplace/plugin entries
- `claude agents` background worker crash
- Plugin MCP same-command different-env dedup
- Plugin git branch update tracking
- Remote MCP egress proxy в Remote sessions
- `cache_creation_input_tokens` reporting 0
- PushNotification tool SDK-hosted incorrect message

## Важливі зміни

- `fallback-model` behavior: тепер switch на fallback для решти сесії замість failure на кожному request
