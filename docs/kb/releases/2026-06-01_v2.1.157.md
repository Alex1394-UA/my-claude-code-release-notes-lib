# Реліз 2.1.157 — 2026-06-01

## Нові можливості
- Плагіни з `.claude/skills` директорій тепер завантажуються автоматично, без необхідності marketplace (категорія: plugins)
- `claude plugin init <name>` — створення шаблону нового плагіна в `.claude/skills` (категорія: cli, plugins)
- Autocomplete для `/plugin` аргументів: підкоманди, встановлені плагіни, плагіни з відомих marketplaces (категорія: cli)
- `claude agents`: поле `agent` в `settings.json` тепер поважається для dispatched sessions; `--agent <name>` для перевизначення (категорія: cli)
- `EnterWorktree` дозволяє перемикатися між Claude-managed worktrees в межах сесії (категорія: cli)
- `tool_decision` telemetry events включають `tool_parameters` при `OTEL_LOG_TOOL_DETAILS=1` (категорія: env-vars)
- Claude worktrees залишаються розблокованими після завершення агента (категорія: cli)
- Feature of the Week credit-claim статус тепер з'являється як notification в status area (категорія: core)
- Workflow keyword trigger setting в `/config` для вимкнення dynamic workflow з prompt (категорія: settings)
- Backspace відхиляє workflow request одразу після trigger keyword (категорія: core)

## Виправлення
- Непридатні зображення стають текстовим placeholder замість крашу сесії (категорія: core)
- Sandbox network prompts не з'являються в auto/bypass-permissions mode (категорія: core)
- `claude agents` завершені сесії retirement fix (категорія: cli)
- `claude agents` Esc cancel fix (категорія: cli)
- Background agent worktrees orphan fix після 30-денної sweep (категорія: cli)
- Background sessions sleep/wake правильна дата (категорія: cli)
- `claude agents` copy-on-select tmux fix (категорія: cli)
- `--resume` background subagents fix (категорія: cli)
- `--resume` picker fullscreen residue fix (категорія: cli)
- `--worktree`/`--worktree --tmux` linked worktree fix (категорія: cli)
- `claude agents` slash-command substring autocomplete (категорія: cli)
- `/model` picker "Newer version available" fix (категорія: core)
- Fullscreen markdown markers fix (категорія: core)
- Terminal freeze managed-settings dialog fix (категорія: core)
- Scrollback duplicate line fix (категорія: core)
- Right-click paste duplicate fix в VS Code/Cursor/Windsurf (категорія: vscode-ide)
- WSL: image paste, screenshot paste Win11, drag images from Explorer (категорія: windows)
- IDE Stop background subagent fix (категорія: vscode-ide)
- VSCode fast mode indicator Opus 4.8 fix (категорія: vscode-ide)
- Покращено продуктивність довгих/відновлених розмов (категорія: core)

## Важливі зміни
- Видалено "bash commands will be sandboxed" startup banner
- Видалено "/ide for …" startup hint toast
