# Реліз 2.1.198 — 2026-07-07

## Нові можливості
- Субагенти запускаються у фоні за замовчуванням — Claude продовжує працювати поки вони виконуються
- Claude in Chrome розширення тепер загальнодоступне (GA)
- Notification hook події `agent_needs_input` та `agent_completed` для `claude agents`
- Новий `/dataviz` skill для дизайну графіків та дашбордів
- Gateway: Claude Platform on AWS (`anthropicAws`) як upstream провайдер; model-not-found просуває failover
- Фонові агенти з `claude agents` тепер автоматично комітять, пушать та відкривають draft PR

## Виправлення
- Explore agent тепер успадковує модель сесії замість haiku
- Субагенти та compaction успадковують extended thinking конфігурацію
- Transient мережеві помилки (ECONNRESET) тепер повторюються з backoff замість відмови
- Надмірні background classifier запити для sandboxed процесів на одному хості
- Фонові задачі застрягали на "Running" в web, desktop та VS Code
- Agent teams: teammate що вмирає від API помилки тепер звітує "failed"
- `/diff` не оновлювався при зміні гілок поза сесією
- Markdown таблиці переповнювались в fullscreen
- AWS/Mantle сесії dead-ending з "Please run /login" — `awsAuthRefresh` автоматично
- "No route to host" для локальних мережевих хостів у macOS background agents
- `/desktop` failing після входу/виходу з worktree
- "Reconnecting…" кожні ~52с у macOS background agents
- `←` в `claude attach` виходив замість відкриття agent view
- `claude --bg` + `--print` мовчазки створював неприєднуєму сесію — тепер відхиляється
- Workflow progress view втрачав агентів в SDK/desktop
- Conditional rules не працювали через symlinked шляхи
- Cmd+click не відкривав URL у Warp fullscreen
- Double-click selection не виділяв URL scheme в fullscreen
- Plan mode не auto-дозволяв read-only tools при старті
- `/branch` брав назву форку з compaction summary
- Keyboard hints показували opt/cmd для Mac через SSH
- API retry UX: причина помилки після 2ї спроби, status page link
- `/login` з agents view
- Subagent message handling improvements
- Видалено `/agents` wizard

## Важливі зміни
- `/agents` wizard видалено — використовуйте Claude або `.claude/agents/` напряму
