# Реліз 2.1.111 — 2026-04-23

## Нові можливості
- Claude Opus 4.7 `xhigh` effort level (категорія: core)
- Auto mode без `--enable-auto-mode` для Max підписників (категорія: core)
- `/effort` інтерактивний слайдер без аргументів (категорія: core)
- `"Auto (match terminal)" тема` в `/theme` (категорія: core)
- `/less-permission-prompts` skill (категорія: skills-agents)
- `/ultrareview` — multi-agent cloud code review (категорія: skills-agents)
- PowerShell tool progressive rollout [Win] (категорія: env-vars, windows)
- Read-only bash glob patterns без permission prompt (категорія: settings)
- `cd <project-dir> &&` без permission prompt (категорія: cli)
- Near-miss subcommand suggestion при typo (категорія: cli)
- Plan файли з іменами промптів (категорія: core)
- `/setup-vertex` та `/setup-bedrock` покращення (категорія: cli)
- `/skills` sort by estimated token count (`t` toggle) (категорія: skills-agents)
- `Ctrl+U` clear entire buffer, `Ctrl+Y` restore (категорія: keybindings)
- `Ctrl+L` forces full screen redraw (категорія: keybindings)
- Transcript view footer shortcuts (категорія: keybindings)
- `plugin_errors` в headless stream-json init event (категорія: sdk)
- `OTEL_LOG_RAW_API_BODIES` env var (категорія: env-vars)
- `CLAUDE_ENV_FILE` та SessionStart hook env files працюють [Win] (категорія: windows)
- Permission rules drive-letter paths коректно anchored [Win] (категорія: windows)

## Виправлення
- Non-streaming retry cap revert (core)
- iTerm2+tmux tearing fix (core)
- `@` file suggestions perf fix (core)
- LSP diagnostics stale fix (core)
- `/resume` tab-complete fix (cli)
- `/context` grid fix (core)
- `/clear` session name fix (core)
- Plugin dependency error improvements (plugins)
- Non-existent `commit` skill fix (skills-agents)
- 429 Bedrock/Vertex status fix (sdk)
- Feedback survey back-to-back fix (core)
- Spurious TUI errors suppressed (core)
