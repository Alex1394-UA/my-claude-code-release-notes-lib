# Реліз 2.1.90 — 2026-04-06

## Нові можливості
- `/powerup` — інтерактивні уроки з анімованими демо (категорія: skills-agents)
- `CLAUDE_CODE_PLUGIN_KEEP_MARKETPLACE_ON_FAILURE` env var для офлайн-середовищ (категорія: env-vars)
- `.husky` додано до захищених директорій (acceptEdits mode) (категорія: settings)

## Виправлення
- Нескінченний цикл rate-limit діалогу після ліміту (категорія: core)
- `--resume` повний prompt-cache miss для deferred tools/MCP (регресія з 2.1.69) (категорія: cli)
- `Edit`/`Write` помилка при PostToolUse format-on-save hook між редагуваннями (категорія: hooks)
- `PreToolUse` hooks з JSON stdout + exit code 2 не блокували інструмент (категорія: hooks)
- Множинний collapsed badge в fullscreen при автозавантаженні CLAUDE.md (категорія: core)
- Auto mode ігнорував явні кордони користувача (категорія: core)
- Click-to-expand текст невидимий на світлих термінальних темах (категорія: core)
- UI краш при malformed tool input в permission dialog (категорія: core)
- Заголовки зникають при скролі `/model`, `/config` (категорія: core)
- Hardened PowerShell permission checks (trailing `&`, `-ErrorAction Break`, TOCTOU, parse-fail) (категорія: windows)

## Продуктивність
- Прибрано per-turn JSON.stringify MCP tool schemas на cache-key lookup (категорія: mcp)
- SSE транспорт: лінійний час для великих стрімів (категорія: mcp)
- SDK сесії: лінійне замість квадратичного уповільнення на транскриптах (категорія: sdk)

## Важливі зміни
- `/resume` паралельне завантаження проєктних сесій (категорія: cli)
- `--resume` picker не показує `-p`/SDK сесії (категорія: cli)
- `Get-DnsClientCache` та `ipconfig /displaydns` прибрано з auto-allow (категорія: windows)
