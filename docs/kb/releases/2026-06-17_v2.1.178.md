# Реліз 2.1.178 — 2026-06-17

## Нові можливості
- `Tool(param:value)` синтаксис permission rules — match параметрів інструменту з `*` wildcard (категорія: settings)
- Вкладені `.claude/skills` тепер завантажуються при роботі з файлами там; при збігу імен nested skill з'являється як `<dir>:<name>` (категорія: skills-agents)
- Nested `.claude/` directory collision resolution — agent/workflow/output-style найближчий до CWD пріоритетний при збігу (категорія: core)

## Виправлення
- Auto mode subagent spawn тепер оцінюється класифікатором перед запуском (категорія: core)
- `/doctor` консистентний flat tree layout (категорія: cli)
- Popередження truncation skill-описів тепер показує кількість обрізаних (категорія: skills-agents)
- Workflow prompt keyword purple shimmer, trigger лише на явних фразах (категорія: skills-agents)
- Remote Control error messages: червоний "/rc failed" indicator, пояснення причин (категорія: sdk)
- `/bug` вимагає опис, не використовує model-refusal як заголовок issue (категорія: cli)
- Crash (out-of-memory) при stale websocket/OAuth FD env var (категорія: core)
- Chrome OAuth різні акаунти (категорія: core)
- Nested skills permission non-interactive fix (категорія: skills-agents)
- Subagent transcript tool results + live progress, повідомлення при завершенні, ctrl+b restart (категорія: skills-agents)
- `claude agents` workers 401 з custom API gateway (категорія: cli)
- Compaction тепер поважає `--fallback-model` (категорія: cli)
- Auth errors після refresh credentials поза сесією (категорія: core)
- Background sessions "Working" forever fix (категорія: cli) — дублікат з 2.1.176
- Linux sandbox symlink `.claude/skills`/`.claude/hooks` (категорія: cli)
- `CLAUDE_CODE_PLUGIN_KEEP_MARKETPLACE_ON_FAILURE=1` fresh install (категорія: plugins)
- MCP server-level specs в subagent `disallowedTools` (категорія: mcp)
- Vim undo `u` крокує по командах (категорія: keybindings)
- Statusline links custom URI schemes (категорія: sdk)
- VSCode Esc dismiss CJK IME (категорія: vscode-ide)
