# Реліз 2.1.94 — 2026-04-13

## Нові можливості
- `CLAUDE_CODE_USE_MANTLE` — Amazon Bedrock через Mantle (категорія: env-vars)
- Compact Slack `#channel` header з клікабельним посиланням (категорія: mcp)
- `keep-coding-instructions` для plugin output styles (категорія: plugins)
- `hookSpecificOutput.sessionTitle` для `UserPromptSubmit` хуків (категорія: hooks)
- Plugin skills `"skills": ["./"]` використовують frontmatter `name` (категорія: plugins)

## Виправлення
- Агенти зависали після 429 з довгим Retry-After (категорія: core)
- Console login macOS мовчазно fail при заблокованому keychain (категорія: cli)
- Plugin skill hooks YAML ігнорувалися (категорія: plugins)
- Plugin hooks "No such file" без `CLAUDE_PLUGIN_ROOT` (категорія: plugins)
- `${CLAUDE_PLUGIN_ROOT}` резолвив до marketplace source (категорія: plugins)
- Scrollback повторював diff / порожні сторінки (категорія: core)
- Multiline промпти відступали під `❯` (категорія: core)
- Shift+Space вставляв "space" в пошуку (категорія: core)
- Гіперпосилання відкривали 2 вкладки в tmux+xterm.js (категорія: core)
- Alt-screen ghost lines (категорія: core)
- `FORCE_HYPERLINK` ігнорувався з settings.json env (категорія: env-vars)
- Native cursor не слідкував за tab (screen readers) (категорія: core)
- Bedrock Sonnet 3.5 v2 — `us.` inference profile (категорія: core)
- SDK/print mode не зберігав partial response при interrupt (категорія: sdk)
- CJK/multibyte U+FFFD в stream-json (категорія: sdk)
- [VSCode] Cold-open subprocess, dropdown wrong item, settings.json parse warning (категорія: vscode-ide)
