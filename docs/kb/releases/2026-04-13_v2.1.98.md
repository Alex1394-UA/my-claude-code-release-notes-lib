# Реліз 2.1.98 — 2026-04-13

## Нові можливості
- Vertex AI setup wizard з екрану логіну (категорія: cli)
- `CLAUDE_CODE_PERFORCE_MODE` — Edit/Write fail на read-only з `p4 edit` підказкою (категорія: env-vars)
- Monitor tool для стрімінгу подій з background скриптів (категорія: core)
- Subprocess sandboxing PID namespace на Linux (категорія: env-vars)
- `--exclude-dynamic-system-prompt-sections` для cross-user prompt caching (категорія: cli)
- LSP `clientInfo` в initialize request (категорія: core)

## Виправлення (без дублікатів з 2.1.97)
- Bash permission bypass через backslash-escaped flag (категорія: settings)
- Compound Bash commands bypassing forced prompts (категорія: settings)
- Bash wildcard rules з extra spaces/tabs (категорія: settings)
- Bash deny rules piped cd downgrade (категорія: settings)
- Bash false prompts для cut/paste/column/awk (категорія: settings)
- Bash `/dev/tcp|udp` redirects не промптили (категорія: settings)
- Bash `grep -f FILE`/`rg -f FILE` поза cwd (категорія: settings)
- Agent team permission mode inheritance (категорія: settings)
- Stalled streaming fallback до non-streaming (категорія: core)
- Capital letters lowercase kitty keyboard protocol (категорія: core)
- macOS text replacements deleting trigger word (категорія: core)
- `/export` не працював з absolute paths/~, переписував extensions (категорія: core)
- `/effort max` denied для unknown model IDs (категорія: core)
- MCP `maxResultSizeChars` не bypassing token persist (категорія: mcp)
- Voice mode leaking spaces on re-hold push-to-talk (категорія: core)
- `DISABLE_AUTOUPDATER` не повністю пригнічував npm check (категорія: env-vars)
- Remote Control permission handler memory leak (категорія: sdk)
- Background subagents partial progress (категорія: core)
- Stale subagent worktree cleanup з untracked files (категорія: core)
- Sandbox mktemp "No such file" після fresh boot (категорія: settings)
- `/agents` tabbed layout (Running/Library) (категорія: skills-agents)
- Vim mode `j`/`k` navigate history (категорія: core)
- Hook errors include stderr first line (категорія: hooks)
- [VSCode] "requires git-bash" false positive [Win] (категорія: windows)
- `CLAUDE_CODE_MAX_CONTEXT_TOKENS` + `DISABLE_COMPACT` (категорія: env-vars)

## Важливі зміни
- `/reload-plugins` підхоплює plugin skills (категорія: plugins)
- OTEL tracing interaction spans (категорія: core)
