# Реліз 2.1.97 — 2026-04-13

## Нові можливості
- Focus view toggle (`Ctrl+O`) в NO_FLICKER mode (категорія: core)
- `refreshInterval` status line setting (категорія: settings)
- `workspace.git_worktree` status line JSON input (категорія: settings)
- `/agents` running indicator `● N running` (категорія: skills-agents)
- Syntax highlighting для Cedar `.cedar`/`.cedarpolicy` (категорія: core)

## Виправлення
- `--dangerously-skip-permissions` понижувався до accept-edits (категорія: settings)
- Hardened Bash tool permissions (категорія: settings)
- Permission rules з JS prototype properties ігнорували settings.json (категорія: settings)
- Managed allow rules залишались після видалення адміном (категорія: settings)
- `permissions.additionalDirectories` mid-session (категорія: settings)
- MCP HTTP/SSE buffer leak ~50MB/год (категорія: mcp)
- MCP OAuth `authServerMetadataUrl` не працював при token refresh (категорія: mcp)
- 429 retries спалювали спроби за ~13с (категорія: core)
- Rate-limit options зникали після compaction (категорія: core)
- `/resume` picker множинні виправлення (категорія: cli)
- File-edit diffs зникали на `--resume` >10KB (категорія: cli)
- Повідомлення під час роботи не зберігались (категорія: core)
- Stop/SubagentStop хуки на довгих сесіях (категорія: hooks)
- Subagent worktree cwd leak (категорія: core)
- Compaction дублювала multi-MB subagent transcripts (категорія: core)
- Plugin update "already latest" для git marketplace (категорія: plugins)
- Slash command picker YAML boolean name (категорія: plugins)
- NO_FLICKER множинні виправлення (URL, scroll, crash, memory leak, Warp, <24 rows) (категорія: core)
- Bedrock SigV4 з порожніми strings (категорія: core)
- NO_FLICKER повільне mouse-wheel [Win] (категорія: windows)
- NO_FLICKER CJK/Unicode при копіюванні [Win] (категорія: windows)

## Продуктивність
- Image compression для pasted/attached images (категорія: core)
- Transcript size optimization (категорія: core)

## Важливі зміни
- Accept Edits auto-approve safe env var wrappers (категорія: settings)
- Auto/bypass auto-approve sandbox network (категорія: settings)
- `sandbox.network.allowMachLookup` працює на macOS (категорія: settings)
- CJK `/` та `@` prompt completion (категорія: core)
- Footer layout indicators on mode row (категорія: core)
- Context-low warning як transient footer (категорія: core)
- Markdown blockquotes continuous left bar (категорія: core)
- Bash OTEL `TRACEPARENT` subprocess env (категорія: core)
- `/claude-api` skill оновлено для Managed Agents (категорія: skills-agents)
