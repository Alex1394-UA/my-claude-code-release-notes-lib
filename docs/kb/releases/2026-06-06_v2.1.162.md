# Реліз 2.1.162 — 2026-06-06

## Нові можливості

- `claude agents --json` включає `waitingFor` що показує на чому очікує сесія
- `--tools` з явним переліком Grep/Glob тепер надає вбудовані search tools на native builds
- `/effort` підтверджує коли обраний рівень збережеться як default
- Slash command autocomplete: клік вставляє в prompt замість негайного запуску
- Remote Control footer pill замість startup message
- MCP per-server timeout <1000ms: sub-1000ms values ігноруються замість floor до 1s watchdog

## Виправлення

- Config directory read-only/unwritable: silent hang → in-memory config з errors
- WebFetch permission rules не застосовувались до preapproved domains
- Windows permission rules ніколи не match з backslashes або case-variant paths
- Read deny rules не ховали файли з Glob/Grep results
- Interrupt (Esc) silent drop на початку turn в stream-json/SDK
- API 400 `no low surrogate` для classifier side-queries та MCP descriptions з emoji
- Cross-session messaging (SendMessage) ламався з deep TMPDIR
- `claude agents` status text обрізався на 60–120 колонок
- `claude agents` обрізав імена сесій на 40 колонок
- `claude agents` attach bounce після background-service restart
- `claude agents` Ctrl+V image paste не працював
- Backgrounding з ← втрачав розмову коли background service не стартує
- Agents view replies що не вдається відправити втрачались
- Opening running background session stall 5s
- LSP `workspaceSymbol` повертала 0 results — тепер приймає `query`
- Windsurf → Devin Desktop rename

## Покращення

- Quieter startup: notices групуються за severity
- Startup warnings коротші та зрозуміліші
- Launch-prompt warnings закріплені під input
- Failed turns: компактний warning замість multi-line error block
- Background service startup очікує endpoint-security scanning
- Background dispatch spawn errors повідомляють клас помилки
