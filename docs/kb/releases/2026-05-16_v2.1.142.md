# Реліз 2.1.142 — 2026-05-16

## Нові можливості
- `claude agents` конфігураційні прапорці: `--add-dir`, `--settings`, `--mcp-config`, `--plugin-dir`, `--permission-mode`, `--model`, `--effort`, `--dangerously-skip-permissions` (категорія: cli)
- Fast mode тепер використовує Opus 4.7 за замовчуванням (було Opus 4.6); `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE=1` для відкату (категорія: core, env-vars)
- Плагіни з root-level `SKILL.md` тепер показуються як skill (категорія: plugins)
- `/plugin` details pane та `claude plugin details` тепер показують LSP servers плагіна (категорія: plugins)
- `/web-setup` попереджає перед заміною існуючого GitHub App connection (категорія: cli)

## Виправлення
- `MCP_TOOL_TIMEOUT` не підвищував per-request fetch timeout для remote HTTP/SSE MCP серверів (категорія: mcp)
- Background sessions не розпізнавали pre-existing git worktrees (категорія: core)
- Background sessions зникали після macOS sleep/wake (категорія: core)
- Daemon не виходив коректно після оновлення binary (категорія: cli)
- Background agents crash-looping з Claude-in-Chrome extension (категорія: core)
- Клік по посиланнях в `claude agents` session не працював (категорія: cli)
- `claude agents` "v to open in editor" використовував daemon's editor замість shell `$EDITOR`/`$VISUAL` (категорія: cli)
- `claude agents` deadlock на Windows з network drives (категорія: windows, cli)
- Background-color bleed при attaching до `claude agents` з 256-color терміналів (категорія: cli)
- `claude --bg --dangerously-skip-permissions` не зберігався через retire/wake (категорія: cli)
- Session titles deriv-алися з URL коли перше повідомлення — посилання (категорія: core)
- Надлишкові `set_model` запити від remote клієнтів (категорія: core)
- Plugins з `skills: ["./"]` показували хибне "path escapes plugin directory" (категорія: plugins)
- Plugin cache cleanup видаляв active plugin version (категорія: plugins)
- `/plugin` browse pane показував "0 installs" для нових plugins (категорія: plugins)
- Plugin advisories не називали кожен `plugin.json` key що shadows default folder (категорія: plugins)

## Покращення
- Reactive compaction: перша summarize спроба починає з overflow size, уникаючи марної near-full-context спроби (категорія: core)
- Hook config error: prompt-/agent-type hooks для `SessionStart`/`Setup`/`SubagentStart` тепер показують чітку помилку (категорія: hooks)
- Видалено застарілу підказку `/model claude-sonnet-4-20250514` з повідомлень відмови Usage Policy (категорія: core)
