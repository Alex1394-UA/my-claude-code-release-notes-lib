# Реліз 2.1.131 — 2026-05-07

## Виправлення
- VS Code extension failing to activate on Windows через hardcoded build path в bundled SDK (`createRequire` polyfill bug) (vscode-ide, windows)
- Mantle endpoint authentication failing з missing `x-api-key` header (sdk)
